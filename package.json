{
  "name": "llm-router",
  "version": "0.1.0",
  "private": true,
  "description": "LLM router for llama.cpp and Ollama backends, with OpenAI-compatible APIs, an admin dashboard, request history, and telemetry.",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "NODE_ENV=development node --watch src/server.js",
    "test": "node --test test/*.test.js",
    "lint:syntax": "node --check src/server.js && node --check src/responses-api.js && node --check src/model-discovery.js && node --check src/upstream.js && node --check src/native-tools.js"
  },
  "keywords": [
    "llm",
    "router",
    "llama.cpp",
    "openai-compatible",
    "ollama",
    "proxy",
    "telemetry"
  ],
  "license": "UNLICENSED"
}
